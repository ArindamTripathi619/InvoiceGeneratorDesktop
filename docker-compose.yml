version: '3.8'
services:
  web:
    build: .
    image: apexsolar/invoice-generator-desktop:latest
    ports:
      - "5173:80" # maps host 5173 -> container 80 (nginx)
    restart: unless-stopped
    environment:
      - NODE_ENV=production
